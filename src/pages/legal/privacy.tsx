import React, { FC } from 'react'

import Layout from '~components/Layout'

const Privacy: FC<unknown> = (): React.ReactElement => {
  return (
    <Layout>
      <div>Privacy</div>
    </Layout>
  )
}

export default Privacy
