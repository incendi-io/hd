import React, { FC } from 'react'

import Layout from '~components/Layout'

const TermsPage: FC<unknown> = (): React.ReactElement => {
  return (
    <Layout>
      <div>Terms</div>
    </Layout>
  )
}
export default TermsPage
